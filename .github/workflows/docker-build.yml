name: Docker Build and Push

on:
  push:
    branches: [main]
    paths:
      - Dockerfile
      - start.sh
      - Caddyfile
      - config.yaml.template
      - .github/workflows/docker-build.yml
      - .env
  workflow_dispatch:

env:
  IMAGE_NAME: mihomo
  REGISTRY: ghcr.io
  DOCKERHUB_IMAGE_NAME: daitcl/mihomo

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      actions: read
      
    steps:
      # 步骤1：检出代码
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 步骤2：设置构建缓存
      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # 步骤3：加载和验证环境变量
      - name: Load and validate environment variables
        id: set-versions
        run: |
          set -e
          
          # 确保.env文件存在
          if [ ! -f .env ]; then
            echo "::error::.env file not found!"
            exit 1
          fi
          
          # 处理Windows换行符
          sed -i 's/\r$//' .env
          
          # 安全加载变量并验证必需字段
          required_vars=("MI_VERSION" "MetaCubeX_VERSION")
          for var in "${required_vars[@]}"; do
            value=$(grep -E "^${var}=" .env | cut -d '=' -f2- | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
            if [ -z "$value" ]; then
              echo "::error::Missing or empty required variable: $var in .env file"
              exit 1
            fi
            echo "${var}=${value}" >> $GITHUB_ENV
            echo "✓ Set ${var}=${value}"
          done
          
          # 设置镜像完整名称
          echo "IMAGE_FULL_NAME=${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}" >> $GITHUB_ENV
          echo "✓ Set IMAGE_FULL_NAME=${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"

      # 步骤4：验证版本可用性
      - name: Verify versions
        run: |
          set -e
          echo "=== Version Verification ==="
          
          # 验证 MetaCubeX 版本
          metacubexd_version="${{ env.MetaCubeX_VERSION }}"
          echo "Verifying MetaCubeX version: $metacubexd_version"
          metacubexd_url="https://api.github.com/repos/MetaCubeX/metacubexd/tags"
          if ! curl -s -H "Accept: application/vnd.github.v3+json" "$metacubexd_url" | jq -e --arg version "$metacubexd_version" 'any(.[]; .name == $version)' > /dev/null; then
            echo "::error::MetaCubeX version $metacubexd_version does not exist!"
            exit 1
          fi
          echo "✓ MetaCubeX version $metacubexd_version is valid"
          
          # 验证 MI 版本
          mi_version="${{ env.MI_VERSION }}"
          echo "Verifying MI version: $mi_version"
          mi_releases_url="https://api.github.com/repos/MetaCubeX/mihomo/releases"
          if ! curl -s -H "Accept: application/vnd.github.v3+json" "$mi_releases_url" | jq -e --arg version "$mi_version" 'any(.[]; .tag_name == $version)' > /dev/null; then
            echo "::error::MI version $mi_version does not exist!"
            exit 1
          fi
          echo "✓ MI version $mi_version is valid"
          
          # 验证 MI 二进制文件是否存在
          mi_binary_url="https://github.com/MetaCubeX/mihomo/releases/download/${mi_version}/mihomo-linux-amd64-compatible-${mi_version}.gz"
          if ! curl -f -I -s -o /dev/null "$mi_binary_url"; then
            echo "::error::MI binary not found at: $mi_binary_url"
            exit 1
          fi
          echo "✓ MI binary is available"

      # 步骤5：生成发布说明
      - name: Generate release notes
        run: |
          sed -e "s/{{MI_VERSION}}/${{ env.MI_VERSION }}/g" \
              -e "s/{{MetaCubeX_VERSION}}/${{ env.MetaCubeX_VERSION }}/g" \
              release_notes.md.template > release_notes.md
          
          echo -e "\n### 镜像URL" >> release_notes.md
          echo "- **GitHub Container Registry:** \`${{ env.IMAGE_FULL_NAME }}:${{ env.MI_VERSION }}\`" >> release_notes.md
          echo "- **Docker Hub:** \`${{ env.DOCKERHUB_IMAGE_NAME }}:${{ env.MI_VERSION }}\`" >> release_notes.md

      # 步骤6：登录容器注册表
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 步骤7：设置 Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        id: buildx
        with:
          driver: docker-container
          buildkitd-flags: --allow-insecure-entitlement network.host

      # 步骤8：构建并推送镜像
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_FULL_NAME }}:${{ env.MI_VERSION }}
            ${{ env.IMAGE_FULL_NAME }}:latest
            ${{ env.DOCKERHUB_IMAGE_NAME }}:${{ env.MI_VERSION }}
            ${{ env.DOCKERHUB_IMAGE_NAME }}:latest
          build-args: |
            MI_VERSION=${{ env.MI_VERSION }}
            MetaCubeX_VERSION=${{ env.MetaCubeX_VERSION }}
          platforms: linux/amd64
          labels: |
            org.opencontainers.image.version=${{ env.MI_VERSION }}
            org.opencontainers.image.description=Mihomo proxy core with Metacubexd dashboard
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.licenses=MIT
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          no-cache: false

      # 步骤9：更新 Docker Hub 描述
      - name: Update Docker Hub description
        uses: peter-evans/dockerhub-description@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.DOCKERHUB_IMAGE_NAME }}
          readme-filepath: ./release_notes.md

      # 步骤10：显示构建摘要
      - name: Show build summary
        run: |
          echo "=== 🎉 Build Successful ==="
          echo ""
          echo "📦 Image Details:"
          echo "  - Version: ${{ env.MI_VERSION }}"
          echo "  - Metacubexd: ${{ env.MetaCubeX_VERSION }}"
          echo "  - Image Size: $(docker images ${{ env.DOCKERHUB_IMAGE_NAME }}:${{ env.MI_VERSION }} --format '{{.Size}}')"
          echo ""
          echo "📍 Registry URLs:"
          echo "  - GHCR: https://${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/pkgs/container/${{ env.IMAGE_NAME }}"
          echo "  - Docker Hub: https://hub.docker.com/r/${{ env.DOCKERHUB_IMAGE_NAME }}"
          echo ""
          echo "🚀 Quick Start:"
          echo "  docker run -d -p 8080:8080 -p 7890:7890 ${{ env.DOCKERHUB_IMAGE_NAME }}:${{ env.MI_VERSION }}"

      # 步骤11：清理缓存（可选）
      - name: Cleanup cache
        if: always()
        run: |
          echo "Build completed, cache will be automatically managed by GitHub Actions"